# Copy this file to .env before running `npm run dev` against a deployed backend.
# Default (no .env file present) uses http://localhost:8000 — no setup needed for local dev.

# URL of the deployed FastAPI backend (no trailing slash)
VITE_API_URL=http://localhost:8000
