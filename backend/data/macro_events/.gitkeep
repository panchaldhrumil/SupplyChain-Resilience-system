# This file keeps the macro_events/ directory tracked by git.
# Actual CSV/JSON data files are excluded via .gitignore (generated by live_macro_pipeline.py).
# Run the pipeline locally to populate this directory:
#   python live_macro_pipeline.py --no-db
